<%= form_for(@medical_team) do |f| %>
  <% if @medical_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_team.errors.count, "error") %> prohibited this medical_team from being saved:</h2>

      <ul>
      <% @medical_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :es_valido %><br>
    <%= f.check_box :es_valido %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
