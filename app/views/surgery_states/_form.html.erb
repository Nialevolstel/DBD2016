<%= form_for(@surgery_state) do |f| %>
  <% if @surgery_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@surgery_state.errors.count, "error") %> prohibited this surgery_state from being saved:</h2>

      <ul>
      <% @surgery_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_estado %><br>
    <%= f.text_field :nombre_estado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
